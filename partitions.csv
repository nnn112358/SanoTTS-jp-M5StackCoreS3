# sanoTTS-jp / M5Stack CoreS3 のパーティション表（16 MB flash、端末内漢字 G2P つき）
#
# 重み blob は app の .rodata に埋めるので model パーティションは無い。
# 辞書（k1-dict-438750.bin、13,702,320 B）は `dict` パーティションを esp_partition_mmap して
# そのまま読む（コピーしない）。
#
# ⚠️ **`dict` の offset は 64 KB (0x10000) 境界に置くこと**（mmap のページ境界 = k1dict の
#    16 バイト境界を満たす）。
# ⚠️ **factory は 2 MB。** app は約 1.5 MB（顔・フォント・Open JTalk 込み）。
#    16 MB − 0x210000 = 14,614,528 B が dict に使える（辞書 13,702,320 B に対し 912 KB の余裕）。
# ⚠️ CoreS3 は PSRAM 8 MB が同じ MMU 窓（32 MB）を使うが、実測で flash mmap の空きは 22.8 MB
#    あった（起動ログ `flash mmap の最大連続空き`）。
#
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0x200000,
dict,     data, 0x41,    0x210000, 0xDF0000,
